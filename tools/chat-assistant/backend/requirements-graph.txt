# LangGraph dependencies for AtlasMind ContractOps
# Installed separately from main requirements to allow easy rollback.
# Only needed when agent.runtime.* config is set to "langgraph".

# Locked to specific minor versions per PRD dependency management rules.
langgraph>=0.4.0,<0.5.0
langgraph-checkpoint>=2.0.0,<3.0.0

# Already in main requirements; listed here for clarity
# pydantic>=2.0
